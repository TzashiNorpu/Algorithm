var minDeletions = function (s) {
    const map = new Map();
    let pos = -1;
    let max = -1;
    for (let i = 0; i < s.length; i++) {
    }
    if (pos == -1) return 0;
    else return i - max;
};